# 第03课实验结果

> 这份讲稿不按“应该发生什么”空讲，而是对照 **已经跑完的** `notebook03.ipynb` 讲。  
> 运行环境：Kaggle CPU，PyTorch `2.10.0+cpu`，全程 `CUDA used: False`，整份笔记本约 **23 秒** 跑完。  
> 上课时左边打开 `notebook03.ipynb`，右边照这份讲。数字、曲线、结论都以该笔记本的输出为准。

---

看笔记本最上面的公式：

\[
\hat{t}_c = w \cdot t_u + b
\]

任务看起来很土：用 11 个点，把未知温标读数换成摄氏度。真正要证明的不是这条直线本身，而是下面这条训练链：

```text
前向预测 → 算 MSE → 求梯度 → 用学习率更新参数
```

笔记本用同一种模型、同一组数据，做了三次实现：

| 实现 | 梯度怎么来 | 参数怎么改 | 本笔记本最终结果 |
|------|------------|------------|------------------|
| 实验1 手工梯度 | 人手写链式法则 | `params - lr * grad` | `w=5.367084, b=-17.301189`，loss=`2.927647` |
| 实验2 autograd | `loss.backward()` | 仍手写减法 | `w=5.367085, b=-17.301193`，loss=`2.927648` |
| 实验3 SGD | `loss.backward()` | `optimizer.step()` | `w=5.367085, b=-17.301193`，loss=`2.927647` |

请把这三行最终参数写在黑板上。课结束前会回来看：三者几乎是同一条直线。最后一格测得手工与 autograd 的最大差只有 **`0.00000381`**。

---

## 1. 环境与数据：先把“战场”看清楚

### 1.1 环境格的输出

```text
PyTorch version: 2.10.0+cpu
Execution device: cpu
CUDA used: False
输出图片目录: /kaggle/working/outputs
```

课堂上强调三点：

1. 这节课不拼算力。线性模型 11 个点，CPU 23 秒足够。
2. 代码把设备锁死为 CPU，所以即使机器有 GPU，结果也和这份笔记本一致。
3. 随机种子是 `20260902`。后面验证集下标全班应相同：训练 `[3,1,4,8,0,9,7,5,6]`，验证 `[2,10]`。

### 1.2 数据格的输出

未知读数 `t_u` 大约 22～82，摄氏度 `t_c` 大约 -4～28。  
`t_un = 0.1 * t_u` 之后变成大约 2.2～8.2。

请学生立刻做一个心算：

- 若瞎猜 `w=1, b=0`，预测值就等于 `t_u`
- 用 55.9 去预测 14.0，显然差很远

后面实验1会给出定量结果：这个瞎猜的 MSE 是 **`1763.88`**。

`t_un` 现在先记住。等会儿会看到：同样 `lr=1e-2`，对 `t_u` 会炸，对 `t_un` 却能学到 `loss≈2.93`。

---

## 2. 先把六个函数讲成一条流水线

模型、损失、手工梯度这一格没有打印数字，但必须讲透，否则后面的 `4517`、`inf`、`2.93` 都没有来历。

黑板上画：

```text
x, w, b  --model-->  ŷ
ŷ, y     --loss_fn-->  L
L 对 ŷ   --dloss_fn-->  每个点该把预测拉高还是拉低
ŷ 对 w   --dmodel_dw-->  x
ŷ 对 b   --dmodel_db-->  1
合起来    --grad_fn-->   (∂L/∂w, ∂L/∂b)
```

MSE 对单个预测的导数是 \(\frac{2}{N}(\hat{y}-y)\)，所以 `dloss_fn` 里有 `/ predictions.size(0)`。  
链式法则就是：损失先告诉预测，预测再告诉 `w` 和 `b`。深度网络只是把中间层加长，原理不变。

四种训练循环也不必逐行念，只对照一件事：**人干的活越来越少，圈还是那一圈。**

---

## 3. 实验1：数字比口号更有说服力

运行实验1后，不要从头读到尾。按下面六段截着讲。

### 3.1 初始模型有多差

```text
初始predictions: 35.7, 55.9, 58.2, 81.9, ...   （就是 t_u 本身）
初始MSE: 1763.884766
```

`w=1, b=0` 等于宣布“未知读数就是摄氏度”。  
1763 不是抽象的“很大”，它的量级来自：预测值在几十，真实值在十几，平方后再平均。

**课堂上的话：** 模型写成直线，并不等于已经学会。没训练时，参数就是瞎猜。

### 3.2 broadcasting 只需一分钟

输出形状：

```text
x * y     -> (3, 1)
y * z     -> (3, 3)
y * z * a -> (2, 3, 3)
```

这是在解释为什么 `w * inputs + b` 不用写 for 循环。标量和向量能自动对齐。主线仍是梯度下降，这里点到为止。

### 3.3 有限差分：梯度原来是“试探”

输出：

```text
有限差分： dL/dw=4517.297363, dL/db=-4600.000000
updated w=-44.172974, b=46.000000
解析gradient: tensor([-256406.296875, -4505.319824])
```

先讲人话：把 `w` 加减 `0.1`，看损失怎么变，再除以 `0.2`。  
得到 `dL/dw≈4517`，意思是：`w` 再增大，损失会猛增，所以必须把 `w` 减小。  
一步 `lr=0.01` 之后，`w` 从 1 直接被打到 **-44.17**。步子已经大得离谱。

然后代码在这个已经被打飞的点上，又算了解析梯度，于是出现 **-256406** 这种更夸张的数。  
**不要把 4517 和 -256406 当成互相矛盾。** 它们不是同一点上的导数：

- `4517` 是在初始点 `(w=1, b=0)` 附近估的
- `-256406` 是在更新后的 `(-44.17, 46)` 上算的

请马上对照实验2的第一次 `backward()`：

```text
第一次backward后的params.grad: tensor([4517.296875, 82.599998])
```

`∂L/∂w` 与有限差分的 `4517.297363` 对到了小数点后两位。  
这是整节课第一个“信任证据”：**autograd 和数值差分在同一点上说的是同一件事。**

`∂L/∂b` 有限差分得到 `-4600`，autograd 得到 `82.6`，对不上。原因也要讲：有限差分是先改了 `w` 再去估 `b`，而 autograd 是在原参数 `(1, 0)` 上同时对两个参数求导。估 `b` 时点已经变了，所以 `dL/db` 不能拿来当验证。**验证自动求导，要用同一点、同一次前向。**

### 3.4 对照 A / B / C：这张表就是这节课的高潮

把实验输出抄成表：

| 设置 | Epoch 1 | Epoch 2 | Epoch 3 | Epoch 10 | Epoch 100 | 结局 |
|------|---------|---------|---------|----------|-----------|------|
| A 原始输入 `lr=1e-2` | 1763.88 | **580 万** | **1.94×10¹⁰** | 大到 10³⁴ 量级 | 没跑到 | **第 11 轮 `inf`，停止** |
| B 原始输入 `lr=1e-4` | 1763.88 | 323.09 | 78.93 | 29.11 | **29.02** | 稳住了，但卡在约 29 |
| C 归一化 `lr=1e-2` | **80.36** | 37.57 | 30.87 | 29.03 | **22.15** | 仍在下降 |

对着表讲三句话：

1. **A 证明学习率不是越大越好。**  
   第 1 轮还是 1763，第 2 轮已经 580 万，第 3 轮 194 亿，第 11 轮直接 `inf`。  
   原因：`t_u` 是几十，`∂L/∂w` 一开始就有 4517，再乘 `0.01`，一步把 `w` 摔出合理范围，之后每步更炸。

2. **B 证明“能稳住”不等于“学得好”。**  
   `lr` 缩小 100 倍后，前 3 轮从 1763 降到 79，看起来很漂亮；但从第 10 轮到第 100 轮，loss 只从 29.11 挪到 29.02。  
   条件数很差：`w` 的梯度被大输入放大，`b` 的梯度相对小，同一个学习率很难同时把两个参数都走到位。所以它几乎趴在 29 附近。

3. **C 证明先改输入尺度，往往比死调学习率更有效。**  
   只做了 `t_un = 0.1 * t_u`。同样 `lr=1e-2`，起点就只有 80.36（预测不再是 55 对 14，而是 5.59 对 14），第 100 轮到 22.15，而且还在降。  
   模型没变，损失没变，变的只是数字范围。

### 3.5 5000 轮完整拟合：参数是怎么爬到谷底的

关键节点：

```text
Epoch    1  loss=80.36    params=( 1.776,   0.106)    grad=(-77.61, -10.64)
Epoch  100  loss=22.15    params=( 2.755,  -2.516)    grad=(-0.445,  2.517)
Epoch 4000  loss= 2.92768 params=( 5.364, -17.285)    grad=(-0.00059, 0.0033)
Epoch 5000  loss= 2.92765 params=( 5.367, -17.301)    grad=(-0.00011, 0.00061)
```

请学生看两件事：

- **loss 先快后慢。** 1→100 轮从 80 降到 22；4000→5000 轮只从 2.92768 降到 2.92765。靠近谷底时，梯度几乎为 0，更新自然变小。
- **梯度在自我关闭。** 第 1 轮 `∂L/∂w=-77.6`，第 5000 轮只剩 `-0.00011`。不是我们把学习率改小了，是已经快走到最优点。

最终：

\[
\hat{t}_c \approx 5.367 \cdot t_{un} - 17.301 = 0.5367 \cdot t_u - 17.301
\]

这里可以加一句“彩蛋”，学生会记住：  
华氏度换摄氏度的标准公式是

\[
t_c = \frac{5}{9}(t_f - 32) \approx 0.5556\, t_f - 17.78
\]

我们学到的 `0.537` 和 `-17.30` 已经非常接近。也就是说，这支“未知温度计”几乎就是华氏温标。11 个带噪声的点，靠梯度下降，把物理关系找了回来。

### 3.6 三张图怎么读（必须对着笔记本里的图讲）

**图1 `01_temperature_data.png`**  
11 个点从左下到右上：未知读数约 22 对应约 -4℃，约 82 对应约 28℃。点不完全在一条线上，但趋势是直线。所以线性模型这个假设成立。

**图2 `02_manual_linear_fit.png`**  
拟合直线穿过点群中心。两端、中间都有点落在线的上下两侧，没有明显系统性弯曲。这说明 MSE 直线已经够用，不必上二次模型。直线不必穿过每一个点——那才是 MSE 的含义。

**图3 `03_learning_rate_comparison.png`（对数纵轴）**  
- 蓝线：原始输入 + `1e-2`，几步之内冲到 \(10^{34}\)，对应对照 A。  
- 橙线、绿线在对数坐标里几乎贴在一起，都很快落到 \(10^1\) 量级。  
**千万提醒学生：贴在一起不等于一样好。** 对数坐标会压缩 29 和 22 的差别。回到表格：橙线（B）卡在 29；绿线（C）第 100 轮已经 22，后面还能降到 2.93。图3 负责证明“谁会炸”；“谁学得更彻底”要看表，也要看 5000 轮那一段。

---

## 4. 实验2：autograd 没有换算法，只是换了计算工

### 4.1 三次打印，把 `.grad` 讲清楚

```text
backward前 params.grad is None: True
第一次backward后的params.grad: tensor([4517.296875, 82.599998])
zero_后的params.grad: tensor([0., 0.])
```

三句话，对应三个事实：

1. 还没 `backward()` 时，梯度不存在，不是 0，是 `None`。
2. `backward()` **只写梯度，不改参数**。此时 `params` 仍是 `[1, 0]`。
3. `zero_()` 清的是梯度。参数还在，方向被擦掉。不清零，下次反传会把新梯度累加上去。

把 `4517.296875` 和实验1有限差分的 `4517.297363` 并排写。差在千分位，来自有限差分的近似。方向、量级完全一致。

### 4.2 5000 轮曲线，几乎是实验1的复印件

```text
Epoch  500  loss=7.860115
Epoch 1000  loss=3.828538
Epoch 2000  loss=2.957698
Epoch 4000  loss=2.927679
Epoch 5000  loss=2.927647
autograd最终params=(5.367085, -17.301193)
```

请对照实验3里 SGD 的同一组打印：`7.860120, 3.828538, 2.957698, 2.927679, 2.927647`。  
从第 1000 轮起，小数点后六位都对得上。

**讲课时的收束句：**

> `backward()` 替代的是实验1的 `grad_fn`，不是替代梯度下降。  
> 所以它必须、也确实收敛到同一组参数。

---

## 5. 实验3：管家上场，但账还是那本账

### 5.1 单步更新：证明 `step()` 就是那一次减法

原始输入、SGD、`lr=1e-5`，一步之后：

```text
params = [0.954827, -0.000826]
```

现场验算：

\[
w \leftarrow 1 - 10^{-5} \times 4517.297 \approx 0.954827
\]

\[
b \leftarrow 0 - 10^{-5} \times 82.60 \approx -0.000826
\]

完全对上。所以 `optimizer.step()` 对 SGD 来说，就是

```text
params -= lr * params.grad
```

的打包写法。

归一化输入、`lr=1e-2`、标准顺序 `zero_grad → backward → step` 之后：

```text
params = [1.776140, 0.106400]
```

这恰好是实验1完整拟合第 1 轮打印的 `params`。三种写法的第一步已经重合。

### 5.2 SGD 5000 轮：第三条重合的直线

```text
SGD最终params: (5.367085, -17.301193)
最终 loss: 2.927647
```

和 autograd 字母级一致。到这里可以在黑板上把三种实现划等号。

### 5.3 Adam + 原始输入：看起来 `w` 差了 10 倍，其实是同一条直线

```text
Epoch  500  loss=7.612900
Epoch 1000  loss=3.086698
Epoch 1500  loss=2.928578
Epoch 2000  loss=2.927646
Adam最终params: (0.536724, -17.302059)
```

学生很容易慌：SGD 的 `w` 是 5.367，Adam 的 `w` 是 0.537。  
提醒他们：Adam 吃的是 **原始 `t_u`**，没有乘 0.1。

\[
5.367 \times t_{un} = 5.367 \times (0.1\, t_u) = 0.5367\, t_u
\]

`b` 都在 `-17.30`。所以 Adam 2000 轮学到的，仍是同一条华氏→摄氏直线。  
它之所以能直接啃原始输入，是因为自适应步长把大梯度压住了，不必我们手工 `* 0.1`。  
**但不要讲成“以后都用 Adam、都不用归一化”。** 本实验只说明：优化器会改变对尺度的敏感程度。SGD 老实，尺度不对就炸；Adam 皮实，但仍建议归一化。

还要注意速度：Adam 2000 轮就把 loss 打到 2.927646，SGD/autograd 大约 2000 轮还在 2.958，4000 轮才到 2.92768。同一条谷底，Adam 走得更急。

### 5.4 训练 / 验证：9 个点学，2 个点考

打乱后：

```text
train_indices: [3, 1, 4, 8, 0, 9, 7, 5, 6]
val_indices:   [2, 10]
```

验证集对应原始数据里的第 2、10 号点，大约是 `(t_u=58.2, t_c=15)` 和 `(t_u=68.4, t_c=21)`。只有两个点，分数会抖，这是后面读图的伏笔。

实验 A（验证也建图）和实验 B（`no_grad`）的损失打印 **完全一样**：

```text
Epoch    1  train=66.581  val=142.389
Epoch    2  train=38.863  val= 64.043
Epoch    3  train=33.348  val= 39.459
Epoch  500  train= 7.145  val=  9.125
Epoch 1000  train= 3.594  val=  5.311
Epoch 3000  train= 3.013  val=  3.576
```

这正是要的结论：

- 关不关验证集的计算图，**不影响学到的参数**，因为 `backward()` 始终只对 `train_loss` 调用。
- 关掉的意义是省内存、省时间，并防止有人误对验证损失反传。
- A 和 B 第 1 轮验证损失高达 142，是因为那两个点一开始被线性模型估得很偏；随着训练，val 从 142 降到 3.58，说明不是在背 9 个训练点那么简单。

最终参数：

```text
(5.196355, -16.751186)
```

和用全部 11 个点得到的 `(5.367, -17.301)` 略有差别。正常：少了 2 个点，直线会偏一点。loss 也停在 3.01 / 3.58，略高于全数据的 2.93。数据少了，拟合当然差一点。

`set_grad_enabled` 检查：

```text
train requires_grad=True, val requires_grad=False
```

两个 `assert` 都过了。这是工程习惯：训练/验证可以共用一个前向函数，但梯度开关必须分开。

### 5.5 图4怎么读

`04_train_validation_loss.png`（对数纵轴）：

- 橙线一开始有一根尖刺，对应 Epoch 1 的 val=142。
- 随后蓝、橙一起下降，大约 1500 轮后变平。
- 蓝线（训练）最终压在橙线（验证）下面：3.01 vs 3.58。
- **没有出现训练继续降、验证掉头向上。** 本实验模型极简、数据也少，看不到典型过拟合拐弯。不要为了讲过拟合而硬编。
- 验证曲线更“高”一点，符合“没见过的点通常更难”。但只有 2 个验证点，这个差距不要解释得太玄。

课堂上的原则仍是：

> 训练损失用来学，验证损失用来检查有没有学会举一反三。  
> 验证数据可以看，不能学。

---

## 6. 最后一格：用一个数给整节课盖章

```text
manual vs. autograd parameter max difference: 0.00000381
所有实验完成，且全过程仅使用CPU。
生成的文件:
  01_temperature_data.png
  02_manual_linear_fit.png
  03_learning_rate_comparison.png
  04_train_validation_loss.png
```

`0.00000381` 远小于阈值 `1e-4`。  
请把这个数和黑板上三组最终参数放在一起：

| 来源 | w | b | 输入 |
|------|------|------|------|
| 手工梯度 5000 轮 | 5.367084 | -17.301189 | `t_un` |
| autograd 5000 轮 | 5.367085 | -17.301193 | `t_un` |
| SGD 5000 轮 | 5.367085 | -17.301193 | `t_un` |
| Adam 2000 轮 | 0.536724 | -17.302059 | `t_u`（等价于 5.367×0.1） |
| 9/2 划分后的 SGD | 5.196355 | -16.751186 | 只用训练集 |

前三行是同一条直线的三种写法。第四行是同一条直线的另一种尺度。第五行是“少看两个点”后的近似。

**结束句：**

> `zero_grad()` 清旧方向；  
> `backward()` 做的就是实验1的链式法则，本笔记本里它对出了 `4517.30`；  
> `step()` 做的就是实验1的 `参数减去学习率乘梯度`，本笔记本里它第一步对出了 `(1.776, 0.106)`。  
> 所谓学习机制，没有第三种神秘力量。

---

## 7. 建议板书（下课前留在黑板上）

```text
初始瞎猜: w=1, b=0, MSE=1763.88

A  lr=1e-2 + 原始输入     → 第11轮 inf
B  lr=1e-4 + 原始输入     → 100轮仍≈29
C  lr=1e-2 + 0.1*输入     → 100轮≈22，5000轮≈2.93
最终直线:  t_c ≈ 0.537 t_u - 17.30   （很像华氏→摄氏）

手工 ≈ autograd ≈ SGD
差 = 0.00000381
```

---

## 8. 课堂提问（全部能用本笔记本的输出回答）

1. 初始 MSE 为什么是 1763 而不是 80？  
   因为那次用的是原始 `t_u`。换成 `t_un` 后，同样 `w=1,b=0` 的起点是 80.36。

2. 有限差分的 `dL/dw=4517` 可信吗？  
   可信。实验2在同一点上 `backward()` 得到 4517.296875。

3. 对照 B 已经降到 29 了，为什么还说它慢？  
   第 10 轮到第 100 轮几乎不动；完整拟合的真正谷底在 2.93。29 只是半山腰。

4. Adam 的 `w=0.537` 是不是学错了？  
   没有。它用的是 `t_u`，`0.537 * t_u` 等于 `5.367 * t_un`。

5. 实验 A 和 B 的 train/val 数字为什么一字不差？  
   验证前向建不建图，不参与 `backward()`，所以更新轨迹相同。

6. 验证损失一直比训练损失高一点，是过拟合吗？  
   本实验看不到验证损失掉头向上。更合理的解释是：验证只有 2 个点，而且没参与拟合，略高是正常的。

---

## 9. 90 分钟怎么卡住这些结果

| 时间 | 盯住的输出 | 学生必须带走的一句话 |
|------|------------|----------------------|
| 0–10 分 | 公式与数据 | 只有两个参数，但训练循环和深度网络是同一套 |
| 10–25 分 | 初始 MSE=1763，有限差分 4517 | 梯度=参数动一点，损失怎么变 |
| 25–50 分 | 对照表 + 图3 + 最终 `(5.367,-17.30)` | 步子太大就飞；输入太大先缩小 |
| 50–65 分 | `.grad` 从 None 到 4517 再到 0 | `backward` 写梯度，不改参数 |
| 65–80 分 | SGD 与 Adam 最终参数、图4 | `step` 就是打包后的更新；验证只看不学 |
| 80–90 分 | 差 `0.00000381` | 三种实现是同一条学习机制 |

时间不够时，只保三样：**对照 A 的 `inf`、最终直线、最后的 `0.00000381`。**


# 第03课：机器怎么调参数

## 任务：把“奇怪温标”换成“摄氏度”

我们有一组数据：

- `t_u`：奇怪的温标读数，比如 35.7、55.9、81.9；
- `t_c`：真正的摄氏度，比如 0.5、14、28。

我们想让机器学会一个换算规则：

\[
\hat{t}_c = w \cdot t_u + b
\]

你可以把它想成一条直线：

- `w` 是斜率，控制这条线有多陡；
- `b` 是上下平移，控制这条线整体高一点还是低一点。

一开始我们让机器瞎猜：

```python
w = 1
b = 0
```

意思就是：机器直接说“你那个温标读数就是摄氏度”。

结果输出：

```text
初始 MSE: 1763.884766
```

MSE 就是“平均犯错程度”。  
1763 非常大，说明机器错得离谱。

这很正常，因为它还没学。

---

## 三、实验1：手工调参数，看看学习率有多重要

### 1. 梯度：告诉机器往哪边调

机器要知道：`w` 和 `b` 应该变大还是变小？

这个“方向”就是梯度。  
你可以把梯度想成“山坡最陡的方向”。

notebook 先用手工方法算了一下，输出里有一个很重要的数：

```text
有限差分：dL/dw=4517.297363
```

后面 autograd 第一次算出来的梯度也是：

```text
tensor([4517.296875, 82.599998])
```

两个数几乎一样。  
说明我们手算的方向是对的。

但注意：这个坡度非常大，有 4517 那么大。  
坡度大，意味着如果步子迈太大，很容易直接冲下山崖。

---

### 2. 学习率：下山时的步子大小

学习率 `lr` 就是步子大小。

notebook 做了三组对照，非常直观。

#### 第一组：原始输入 + `lr=0.01`

```text
Epoch 1  | loss=1763.884766
Epoch 2  | loss=5802484.500000
Epoch 3  | loss=19408029696.000000
Epoch 11 | loss=inf
```

这就是步子太大，直接飞出去了。  
损失从 1763 变成 580 万，再变成 194 亿，最后变成无穷大。

结论：**学习率太大，会发散。**

#### 第二组：原始输入 + `lr=0.0001`

```text
Epoch 1  | loss=1763.884766
Epoch 2  | loss=323.090515
Epoch 3  | loss=78.929634
Epoch 100| loss=29.022667
```

这次不发散了，但走得很慢。  
100 轮之后，损失还在 29 左右。

结论：**学习率太小，稳定但慢。**

#### 第三组：归一化输入 + `lr=0.01`

我们把输入缩小 10 倍：

```python
t_un = 0.1 * t_u
```

结果：

```text
Epoch 1  | loss=80.364342
Epoch 2  | loss=37.574913
Epoch 3  | loss=30.871077
Epoch 100| loss=22.148710
```

同样 `lr=0.01`，这次不但没发散，还降得更快。

为什么？  
因为原始输入的数值是几十，归一化后变成几。  
数字小了，坡度就没那么陡，机器走起来更舒服。

你可以这样打比方：

> 原始输入像用“厘米”量，数字很大；归一化像用“米”量，数字小了，步子就好控制。

最后跑 5000 轮，机器学到：

```text
params = [5.367084, -17.301189]
loss = 2.927647
```

换回原始输入，大概就是：

\[
\hat{t}_c \approx 0.5367 \cdot t_u - 17.3
\]

这个结果已经比一开始的 1763 好太多了。

---

## 四、实验2：autograd 自动求导，机器自己算方向

刚才梯度是我们手工推的。  
实验2 换成 PyTorch 的 `autograd`：

```python
loss.backward()
```

第一次反向传播后，梯度是：

```text
tensor([4517.296875, 82.599998])
```

和我们手工算的几乎一样。

但这里有个坑：**梯度会累加。**

所以每次反向传播前，要先把旧的梯度清零：

```python
if params.grad is not None:
    params.grad.zero_()
```

不清零，就像你上次欠的钱还没还，这次又往上加，最后就乱了。

更新参数时，还要用：

```python
with torch.no_grad():
    params -= learning_rate * params.grad
```

因为调参数这个动作本身不需要被记录进计算图。

实验2 跑完 5000 轮，结果：

```text
autograd最终params = [5.367085, -17.301193]
loss = 2.927648
```

和手工梯度结果几乎一模一样。

最后对照：

```text
manual vs. autograd parameter max difference: 0.00000381
```

差别只有 0.00000381。  
说明：**autograd 自动求导是靠谱的。**

---

## 五、实验3：optimizer，自动挡开车

实验3 更进一步：连参数更新也不用我们手写了，交给优化器。

notebook 里提到了很多优化器：

```text
SGD, Adam, AdamW, Adagrad, RMSprop ...
```

你可以把优化器想成“自动挡汽车”。

- `SGD`：最基础，手动挡，学习率要自己调好；
- `Adam`：更智能，通常更省心。

notebook 里做了对比：

### SGD 配归一化输入，跑 5000 轮

```text
SGD最终params: [5.367085, -17.301193]
loss = 2.927647
```

### Adam 配原始输入，跑 2000 轮

```text
Adam最终params: [0.536724, -17.302059]
loss = 2.927646
```

注意：Adam 学到的 `w` 是 0.5367，而 SGD 在归一化输入上学到的是 5.367。  
因为归一化时乘了 0.1，所以：

\[
5.367 \times 0.1 \approx 0.5367
\]

本质上它们学到的是同一条直线。

结论：

> SGD 对输入尺度和学习率更敏感；Adam 通常更省心，原始输入也能跑。

---

## 六、训练集和验证集：平时练习和模拟考试

notebook 最后把 11 个数据分成：

- 9 个训练；
- 2 个验证。

训练集就是平时练习题，验证集就是模拟考。  
平时练得好，不代表模拟考一定好，所以要看两个损失。

输出：

```text
Epoch 1    | training loss=66.581085 | validation loss=142.389008
Epoch 3000 | training loss=3.012515 | validation loss=3.575581
```

训练损失最后是 3.01，验证损失是 3.57。  
验证损失高一点很正常，因为验证集只有 2 个点，随机性很大。

notebook 还比较了两种验证方式：

- 一种验证时也建计算图；
- 一种用 `torch.no_grad()`。

结果数值完全一样，但 `no_grad` 更省内存、更快。

最后还检查了：

```text
set_grad_enabled检查： train requires_grad=True, val requires_grad=False
```

也就是说：

- 训练时，需要梯度，方便反向传播；
- 验证时，不需要梯度，省点力气。

---

## 七、总结：今天记住五句话就够了

1. **机器学习，就是不断试错，然后往错误变小的方向调参数。**
2. **梯度告诉你往哪调，学习率告诉你每次调多大。**
3. **步子太大会发散，步子太小会太慢；输入归一化能让步子更好走。**
4. **autograd 自动算梯度，和手工算结果一致，但记得清空旧梯度。**
5. **optimizer 是自动挡；训练集用来学，验证集用来检查有没有学偏。**

---

## 八、课堂可以问学生的问题

1. 为什么一开始 MSE 是 1763，而不是几十？
2. 为什么原始输入配大学习率会爆炸？
3. 为什么输入归一化之后，同样学习率就不爆炸了？
4. 为什么每次反向传播前要清零梯度？
5. 为什么 Adam 在原始输入上学到的 `w`，约等于 SGD 在归一化输入上学到的 `w` 的 0.1 倍？

---

## 九、课后小练习

1. 把学习率改成 `0.1`、`0.001`、`0.00001`，看看损失怎么变。
2. 把归一化改成标准化：`(t_u - 平均值) / 标准差`，比较收敛速度。
3. 用 `SGD` 和 `Adam` 都跑原始输入，看谁更稳。
4. 把验证集比例改成 30%，观察训练和验证损失。
5. 画出最后那条拟合直线，解释 `w` 和 `b` 分别是什么意思。

---

这一版讲稿更偏口语化，适合课堂上边演示 notebook 边讲。核心就是让学生看懂：**机器不是一下子变聪明的，它是一步一步根据错误调整参数，慢慢学会的。**
